goto_hotel_scheduled:
  - selector: view
    locator:
      inner_text: IP房
      max_timeout: 30
    action: click
  - selector: .btn-action
    locator:
      inner_text: 立即预订
      max_timeout: 30
    action: click